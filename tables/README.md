# Tables (generated)

These tables are produced from the source feeds by:

    python scripts/run_all.py

The contents are gitignored (the git repository tracks source only); the **released archive bundles
reference copies** so the results can be inspected without re-running the pipeline.

    t1_coverage.csv      observed coverage by service, measurement type, and provenance tier
    t2_breaks.csv        structural-break dates with 90% bootstrap intervals, by mode
    t3_implications.csv  findings -> planning lever -> agency action
