# Socrata / data.ny.gov app token for live pulls. Copy to .env and fill in.
# NEVER commit the real .env (see .gitignore). Rotate any token ever pasted into a notebook.
SOCRATA_APP_TOKEN=your_token_here
